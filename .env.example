# Groq API key (primary provider — Llama 3.1 8B):
GROQ_API_KEY=

# Google Gemini API key (secondary fallback):
# GEMINI_API_KEY=
#
# Without any API keys, the app runs in mock mode with simulated AI responses.
